# Database Connection (PostgreSQL)
# Replace with your actual Postgres connection string (Supabase, Neon, or Local)
# Use your provider's pooler URL for DATABASE_URL if recommended (e.g., Supabase Transaction pooler).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB_NAME?schema=public&sslmode=require"

# Direct connection for migrations (avoid poolers like PgBouncer)
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/DB_NAME?schema=public&sslmode=require"

# NextAuth Configuration
# For production: use your actual domain (https://your-domain.com)
# For local development: http://localhost:3000
NEXTAUTH_URL="https://your-domain.com"

# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NEXTAUTH_SECRET="replace-with-strong-random-secret"

# App Logic
NEXT_PUBLIC_APP_NAME="ArchetypeOS"
